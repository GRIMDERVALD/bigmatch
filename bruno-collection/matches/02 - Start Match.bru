meta {
  name: Start Match
  type: http
  seq: 2
}

post {
  url: {{api_url}}/matches/{{match_id}}/start
  body: none
  auth: none
}

tests {
  test("Match started", function() {
    expect(res.status).to.equal(200);
    expect(res.body.message).to.equal("Match started");
  });
}